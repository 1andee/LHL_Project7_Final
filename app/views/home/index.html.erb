<% if current_user %>
  <%= render 'user_feed' %>
<% else %>

  <section class="home-search">
    <div class="search-input">
      <%= form_tag home_index_path, :method => 'get' do %>
        <input id="searchbar" name="query">
      <% end %>
    </div>
    <%= render partial: 'users/users', locals: {users: @users} %>
  </section>

  <section class="project-showcase">
    <h1>Projects</h1>
  </section>

<% end %>
