console.log('here');

// $('#feeds-list').prepend("<%= raw escape_javascript(render(@feeds)) %>");


// $('#feeds-list').append("<%= (render(@feeds)).to_s.to_json %>");
// $( document ).on('turbolinks:load', function() {
//   $('#feeds-list').append("<%= (render(@feeds)).to_s.to_json %>");
// });

$( document ).on('turbolinks:load', function() {
  $('#feeds-list').prepend("<%= raw escape_javascript(render(@feeds)) %>");
});