<nav class="navbar navbar-default navbar-fixed-top">
  <ul class="nav-left">
    <li><%= link_to 'PairWise', :root, { :class=>"logo" } %></li>
    <li><%= link_to 'Search', users_path %></li>
    <% if current_user %>
    <li><%= link_to 'New Project', new_project_path %></li>
    <% end %>
  </ul>
  <ul class="nav-right">
    <% if current_user %>
      <li>Signed in as <%= link_to current_user.name, user_path(current_user.id) %> | <%= link_to "Logout", '/logout' %></li>
    <% else %>
      <li><%= link_to 'Register/Login',  [:authenticate] %></li>
    <% end %>
  </ul>
</nav>
